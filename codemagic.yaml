workflows:
  android-release:
    name: Android Release Build
    instance_type: linux_x2
    max_build_duration: 120
    environment:
      flutter: stable
      android_signing:
        - choice-keystore
      groups:
        - google_play
        - choice_app_env
      vars:
        PACKAGE_NAME: "fr.choiceapp.app"
    scripts:
      - name: Configurer le fichier .env pour le build
        script: |
          echo "Génération du fichier .env pour l'environnement de build..."
          # Récupérer le chemin du répertoire du projet
          PROJECT_DIR=$(pwd)
          
          # S'assurer que le répertoire frontend existe
          if [ -d "frontend" ]; then
            # Si le répertoire frontend existe, on y accède
            cd frontend
            FRONTEND_DIR=$(pwd)
          else
            # Si on est déjà dans frontend ou il n'existe pas
            FRONTEND_DIR=$(pwd)
          fi
          
          # S'assurer que le répertoire assets/env existe
          mkdir -p "$FRONTEND_DIR/assets/env"
          
          # Créer directement le fichier dans assets/env/
          echo "Création du fichier d'environnement dans: $FRONTEND_DIR/assets/env"
          cat > "$FRONTEND_DIR/assets/env/production.env" << EOL
          # Configuration du serveur
          PORT=5000
          NODE_ENV=production
          
          # Configuration de MongoDB
          MONGO_URI=$CM_MONGO_URI
          
          # Configuration JWT
          JWT_SECRET=$CM_JWT_SECRET
          
          # Configuration des uploads
          UPLOAD_DIR=uploads
          MAX_FILE_SIZE=5242880
          
          # Configuration des APIs externes
          GOOGLE_MAPS_API_KEY=$CM_GOOGLE_MAPS_API_KEY
          STRIPE_SECRET_KEY=$CM_STRIPE_SECRET_KEY
          STRIPE_WEBHOOK_SECRET=$CM_STRIPE_WEBHOOK_SECRET
          OPENAI_API_KEY=$CM_OPENAI_API_KEY
          
          # Base URL pour l'API
          API_BASE_URL=https://api.choiceapp.fr
          WEBSOCKET_URL=wss://api.choiceapp.fr
          EOL
          
          # Copier aussi dans default.env pour être sûr
          cp "$FRONTEND_DIR/assets/env/production.env" "$FRONTEND_DIR/assets/env/default.env"
          
          echo "✅ Fichiers d'environnement créés avec succès pour l'environnement de build"
          
          # Vérifier que les fichiers existent et afficher leur taille
          if [ -f "$FRONTEND_DIR/assets/env/default.env" ] && [ -f "$FRONTEND_DIR/assets/env/production.env" ]; then
            echo "🔍 Les fichiers d'environnement ont été créés correctement:"
            ls -la "$FRONTEND_DIR/assets/env/"
            # Afficher uniquement les noms des variables (pas les valeurs)
            grep -o "^[A-Z_]*=" "$FRONTEND_DIR/assets/env/default.env" | sort
            
            # Vérifier que les fichiers sont accessibles
            if [ -r "$FRONTEND_DIR/assets/env/default.env" ]; then
              echo "✅ Les fichiers d'environnement sont lisibles"
            else
              echo "⚠️ Les fichiers d'environnement ne sont pas lisibles"
            fi
            
            # Retourner au répertoire du projet
            cd "$PROJECT_DIR"
          else
            echo "❌ Erreur: Fichiers d'environnement non créés"
            cd "$PROJECT_DIR"
            exit 1
          fi

      - name: Verify asset in pubspec.yaml
        script: |
          # Vérifier que .env est correctement déclaré dans le pubspec.yaml
          if [ -d "frontend" ]; then
            PUBSPEC_FILE="frontend/pubspec.yaml"
          else
            PUBSPEC_FILE="pubspec.yaml"
          fi
          
          if [ -f "$PUBSPEC_FILE" ]; then
            if grep -q "assets:" "$PUBSPEC_FILE" && grep -q "- .env" "$PUBSPEC_FILE"; then
              echo "✅ .env est correctement déclaré comme asset dans pubspec.yaml"
            else
              echo "⚠️ .env n'est peut-être pas correctement déclaré comme asset dans pubspec.yaml"
              echo "Contenu de la section assets:"
              grep -A 10 "assets:" "$PUBSPEC_FILE"
            fi
          else
            echo "❌ Fichier pubspec.yaml non trouvé: $PUBSPEC_FILE"
          fi

      - name: Clean and Get Dependencies
        script: |
          # Nettoyer et réparer
          flutter clean
          flutter pub cache repair
          
          # Vérifier les versions de flutter_map et flutter_map_marker_cluster
          if grep -q "flutter_map: \^8.0.0" pubspec.yaml; then
            # Ajuster la version de flutter_map pour la compatibilité
            sed -i 's/flutter_map: \^8.0.0/flutter_map: \^7.0.0/g' pubspec.yaml
            echo "✓ Flutter Map ajusté à ^7.0.0 pour compatibilité avec marker_cluster"
          fi
          
          if grep -q "flutter_map_marker_cluster: \^1.3.4" pubspec.yaml; then
            # Mettre à jour flutter_map_marker_cluster pour compatibilité avec flutter_map ^7.0.0
            sed -i 's/flutter_map_marker_cluster: \^1.3.4/flutter_map_marker_cluster: \^1.4.0/g' pubspec.yaml
            echo "✓ Flutter Map Marker Cluster mis à jour à ^1.4.0"
          fi
          
          # Force des URLs HTTPS pour les dépendances Git
          git config --global url."https://".insteadOf "git://"
          git config --global url."https://github.com/".insteadOf "git@github.com:"
          
          # Obtenir les dépendances proprement
          flutter pub get --verbose
          
          # S'assurer que les assets/env existent dans le répertoire de build
          mkdir -p build/app/intermediates/flutter/release/assets/env/
          
          # Copier les fichiers d'environnement dans le répertoire de build
          if [ -d "assets/env" ]; then
            cp -r assets/env/* build/app/intermediates/flutter/release/assets/env/
            echo "✅ Fichiers d'environnement copiés vers le répertoire de build"
            ls -la build/app/intermediates/flutter/release/assets/env/
          else
            echo "⚠️ Répertoire assets/env non trouvé pour la copie"
          fi

      - name: Fix Android Configuration
        script: |
          # Vérifier et corriger les niveaux d'API Android
          GRADLE_FILE="android/app/build.gradle"
          if grep -q "compileSdkVersion 35" "$GRADLE_FILE"; then
            sed -i 's/compileSdkVersion 35/compileSdkVersion 34/g' "$GRADLE_FILE"
            sed -i 's/targetSdkVersion 35/targetSdkVersion 34/g' "$GRADLE_FILE"
            echo "✓ Niveaux d'API Android corrigés (35 -> 34)"
          fi
          
          # Vérifier GradleException et remplacer si nécessaire
          if grep -q "throw new GradleException" "$GRADLE_FILE"; then
            sed -i 's/throw new GradleException/throw new RuntimeException/g' "$GRADLE_FILE"
            echo "✓ GradleException remplacé par RuntimeException"
          fi
          
          # Ajouter les permissions internet dans le manifeste si nécessaire
          MANIFEST_FILE="android/app/src/main/AndroidManifest.xml"
          if [ -f "$MANIFEST_FILE" ] && ! grep -q "android.permission.INTERNET" "$MANIFEST_FILE"; then
            sed -i '/<uses-permission/a \    <uses-permission android:name="android.permission.INTERNET" />' "$MANIFEST_FILE"
            echo "✓ Permission internet ajoutée"
          fi

      - name: Vérification de Compatibilité Web
        script: |
          echo "Vérification de la configuration pour le web..."
          
          # Vérifier la présence du fichier adaptatif pour les cartes
          MAP_UTIL_DIR="lib/widgets/maps"
          if [ ! -d "$MAP_UTIL_DIR" ]; then
            mkdir -p "$MAP_UTIL_DIR"
          fi
          
          # Vérifier que les dépendances nécessaires sont activées pour le web
          WEB_INDEX="web/index.html"
          if [ -f "$WEB_INDEX" ] && ! grep -q "openstreetmap" "$WEB_INDEX"; then
            # Ajouter le lien vers la feuille de style OpenStreetMap si nécessaire
            sed -i '/<\/head>/i \  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"/>' "$WEB_INDEX"
            echo "✓ Feuille de style Leaflet ajoutée pour le web"
          fi

      - name: Build Android Bundle
        script: |
          # Construire le bundle Android
          flutter build appbundle --release
          
          # Vérifier si le build a réussi
          if [ -f "build/app/outputs/bundle/release/app-release.aab" ]; then
            echo "✅ Build Android réussi!"
          else
            echo "❌ Build Android échoué"
            exit 1
          fi

      - name: Verify bundle structure (debug)
        script: |
          echo "Vérification de la structure du bundle..."
          # Vérifier les assets intermédiaires
          if [ -d "build/app/intermediates/flutter/release/assets/env" ]; then
            echo "✅ Dossier assets/env trouvé dans le répertoire des assets intermédiaires"
            echo "Contenu du dossier assets/env:"
            ls -la build/app/intermediates/flutter/release/assets/env/
          else
            echo "⚠️ Dossier assets/env non trouvé dans le répertoire des assets intermédiaires"
            echo "Contenu du répertoire flutter/release:"
            find build/app/intermediates/flutter/release -type d | sort
          fi
          
          # Copier manuellement les fichiers d'environnement si nécessaire
          mkdir -p build/app/intermediates/flutter/release/assets/env
          if [ -f "assets/env/default.env" ]; then
            cp assets/env/default.env build/app/intermediates/flutter/release/assets/env/
            cp assets/env/production.env build/app/intermediates/flutter/release/assets/env/
            echo "✅ Fichiers d'environnement copiés manuellement dans le répertoire des assets intermédiaires"
          else
            echo "⚠️ Impossible de trouver les fichiers d'environnement source pour la copie manuelle"
          fi
          
          # Utiliser l'outil bundletool pour analyser le bundle
          echo "Extraction du contenu du bundle pour vérification..."
          mkdir -p bundle_analysis
          if [ -f "${ANDROID_HOME}/cmdline-tools/latest/bin/bundletool" ]; then
            BUNDLETOOL="${ANDROID_HOME}/cmdline-tools/latest/bin/bundletool"
          else
            # Télécharger bundletool si nécessaire
            if [ ! -f "bundletool.jar" ]; then
              echo "Téléchargement de bundletool..."
              curl -L -o bundletool.jar https://github.com/google/bundletool/releases/download/1.15.6/bundletool-all-1.15.6.jar
            fi
            BUNDLETOOL="java -jar bundletool.jar"
          fi
          
          # Convertir le bundle en APKs pour l'inspection
          $BUNDLETOOL build-apks --bundle=build/app/outputs/bundle/release/app-release.aab --output=bundle_analysis/app.apks
          
          # Extraire les APKs pour l'inspection
          mkdir -p bundle_analysis/extracted
          unzip -q bundle_analysis/app.apks -d bundle_analysis/extracted
          
          # Vérifier la présence de .env dans les assets
          echo "Recherche de .env dans les assets extraits..."
          find bundle_analysis -name "*.env" -type f

    artifacts:
      - build/app/outputs/bundle/release/app-release.aab

    publishing:
      email:
        recipients:
          - "remi.barbier@hec.edu"

  # Version sans signature de code (si jamais vous rencontrez à nouveau des problèmes de profil)
  ios-release-unsigned:
    name: iOS Release Build (Unsigned)
    instance_type: mac_mini_m1
    max_build_duration: 120
    environment:
      flutter: stable
      xcode: latest
      cocoapods: latest
      groups:
        - choice_app_env
      vars:
        # Variables pour désactiver complètement la signature de code
        DISABLE_CODE_SIGNING: "true"
        ENABLE_MANUAL_PROVISIONING: "false"
    scripts:
      - name: Configurer le fichier .env pour le build
        script: |
          echo "Génération du fichier .env pour l'environnement de build..."
          cat > .env << EOL
          # Configuration du serveur
          PORT=5000
          NODE_ENV=production
          
          # Configuration de MongoDB
          MONGO_URI=$CM_MONGO_URI
          
          # Configuration JWT
          JWT_SECRET=$CM_JWT_SECRET
          
          # Configuration des uploads
          UPLOAD_DIR=uploads
          MAX_FILE_SIZE=5242880
          
          # Configuration des APIs externes
          GOOGLE_MAPS_API_KEY=$CM_GOOGLE_MAPS_API_KEY
          STRIPE_SECRET_KEY=$CM_STRIPE_SECRET_KEY
          STRIPE_WEBHOOK_SECRET=$CM_STRIPE_WEBHOOK_SECRET
          OPENAI_API_KEY=$CM_OPENAI_API_KEY
          
          # Base URL pour l'API
          API_BASE_URL=https://api.choiceapp.fr
          WEBSOCKET_URL=wss://api.choiceapp.fr
          EOL
          
          echo "✅ Fichier .env créé avec succès pour l'environnement de build"
          
          # Vérifier que le fichier existe
          if [ -f ".env" ]; then
            echo "🔍 Le fichier .env a été créé correctement"
            # Afficher uniquement les noms des variables (pas les valeurs)
            grep -o "^[A-Z_]*=" .env | sort
          else
            echo "❌ Erreur: Le fichier .env n'a pas été créé"
            exit 1
          fi

      - name: Clean Flutter project
        script: |
          flutter clean
          flutter pub get
          flutter pub cache repair

      - name: Verify Flutter dependencies
        script: |
          if ! grep -q "add_2_calendar" pubspec.yaml; then
            echo "⚠️  La dépendance add_2_calendar semble manquante dans pubspec.yaml."
            exit 1
          fi

      - name: Ensure iOS deployment target is 14.0+ in Podfile
        script: |
          cd ios
          
          if [ -f "Podfile" ]; then
            sed -i '' 's/platform :ios, .*/platform :ios, "14.0"/' Podfile
          else
            echo "🚨 Podfile non trouvé ! Assure-toi que ton projet Flutter iOS est bien configuré."
            exit 1
          fi

      - name: Update CocoaPods
        script: |
          sudo gem install cocoapods --pre
          pod repo update

      - name: Remove and reinstall CocoaPods dependencies
        script: |
          cd ios
          rm -rf Pods Podfile.lock
          pod deintegrate || echo "Pod deintegrate skipped or failed, continuing..."
          
          # Vérifier les chemins des plugins
          echo "🔍 Vérification des dossiers de plugins importants..."
          SYMLINK_DIR=".symlinks/plugins"
          for PLUGIN in "firebase_core" "cloud_firestore" "firebase_core_custom" "cloud_firestore_custom" "add_2_calendar"; do
            echo "  - Checking $PLUGIN..."
            if [ -d "$SYMLINK_DIR/$PLUGIN" ]; then
              echo "    ✅ $PLUGIN directory exists"
              find "$SYMLINK_DIR/$PLUGIN" -name "*.podspec" | xargs ls -la || echo "    ⚠️ No podspec files found"
            else
              echo "    ❌ $PLUGIN directory missing"
            fi
          done
          
          # Forcer l'installation des pods
          echo "🚀 Tentative d'installation des pods..."
          COCOAPODS_DISABLE_STATS=true pod install --repo-update --verbose
          
          # Si l'installation échoue, essayer avec --allow-root et autres options
          if [ $? -ne 0 ]; then
            echo "⚠️ Première tentative d'installation des pods échouée, essai avec options alternatives..."
            COCOAPODS_DISABLE_STATS=true pod install --repo-update --verbose --allow-root
            
            # Si ça échoue encore, essayer avec --no-clean
            if [ $? -ne 0 ]; then
              echo "⚠️ Seconde tentative échouée, essai avec --no-clean..."
              COCOAPODS_DISABLE_STATS=true pod install --repo-update --verbose --no-clean
            fi
          fi
          
          # Vérifier si l'installation a réussi
          if [ -d "Pods" ] && [ -f "Podfile.lock" ]; then
             echo "✅ Installation des pods réussie."
          else
             echo "❌ Échec de l'installation des pods après plusieurs tentatives."
             # Afficher le contenu du répertoire pour le débogage
             ls -la
             
             # Tenter une approche alternative avec bundle
             echo "🔄 Tentative avec bundler..."
             sudo gem install bundler
             bundle init
             echo "gem 'cocoapods', '1.12.1'" >> Gemfile
             bundle install
             bundle exec pod install --repo-update
             
             # Vérifier à nouveau
             if [ -d "Pods" ] && [ -f "Podfile.lock" ]; then
                echo "✅ Installation des pods réussie avec bundler."
             else
                exit 1
             fi
          fi
          
          # Afficher le Pods/Manifest.lock pour déboguer
          if [ -f "Pods/Manifest.lock" ]; then
            echo "📋 Contenu de Pods/Manifest.lock:"
            head -n 50 Pods/Manifest.lock # Afficher les 50 premières lignes
          else
            echo "⚠️ Pods/Manifest.lock non trouvé"
          fi

      - name: Verify Apple Pay Merchant ID in Entitlements
        script: |
          cd ios
          if [ ! -f "Runner/Runner.entitlements" ]; then
            cat > Runner/Runner.entitlements << EOL
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.developer.in-app-payments</key>
    <array>
        <string>merchant.fr.choiceapp.app</string>
    </array>
</dict>
</plist>
EOL
            echo "✅ Fichier d'entitlements Apple Pay créé"
          else
            echo "✅ Fichier d'entitlements existant"
          fi
          if ! grep -q "merchant.fr.choiceapp.app" Runner/Info.plist; then
            plutil -insert 'com.apple.developer.in-app-payments' -xml '<array><string>merchant.fr.choiceapp.app</string></array>' Runner/Info.plist || true
            echo "✅ Merchant ID ajouté à Info.plist"
          fi

      - name: Apply iOS 14.0 Deployment Fix in Xcode Info.plist
        script: |
          cd ios
          plutil -replace MinimumOSVersion -string "14.0" Runner/Info.plist
          plutil -replace LSRequiresIPhoneOS -bool YES Runner/Info.plist

      - name: Install certificate in keychain
        script: |
          echo $CM_CERTIFICATE | base64 --decode > ios_distribution.p12
          security create-keychain -p "" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "" build.keychain
          security import ios_distribution.p12 -k build.keychain -P "$CM_CERTIFICATE_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple: -s -k "" build.keychain
          echo "✅ Certificat installé dans le keychain"

      - name: Debug keychain and provisioning
        script: |
          echo "== Profils de provisionnement =="
          ls -l ~/Library/MobileDevice/Provisioning\ Profiles/
          echo "== Certificats dans le keychain =="
          security find-identity -v -p codesigning
          echo "== Team ID utilisé =="
          echo $CM_APP_STORE_CONNECT_TEAM_ID

      - name: Build iOS IPA with Proper Signing
        script: |
          flutter build ipa --release
          echo "✅ Build iOS avec signature terminé"
          if [ -f "build/ios/ipa/app-release.ipa" ]; then
            echo "🎉 IPA signé généré avec succès"
          else
            echo "🔍 Recherche d'autres IPAs générés..."
            find build -name "*.ipa"
          fi

    artifacts:
      - build/ios/ipa/*.ipa

    publishing:
      email:
        recipients:
          - "remi.barbier@hec.edu"

  # Version avec signature de code (avec les certificats et profils mis à jour)
  ios-release:
    name: iOS Release Build (Signed)
    instance_type: mac_mini_m1
    max_build_duration: 120
    environment:
      flutter: stable
      xcode: latest
      cocoapods: latest
      ios_signing:
        distribution_type: ad-hoc
        bundle_identifier: fr.choiceapp.app
        team_id: $CM_APP_STORE_CONNECT_TEAM_ID
        provisioning_profiles:
          - fr.choiceapp.app: CM_PROVISIONING_PROFILE
    scripts:
      - name: Configurer le fichier .env pour le build
        script: |
          echo "Génération du fichier .env pour l'environnement de build..."
          # Récupérer le chemin du répertoire du projet
          PROJECT_DIR=$(pwd)
          
          # S'assurer que le répertoire frontend existe
          if [ -d "frontend" ]; then
            # Si le répertoire frontend existe, on y accède
            cd frontend
            FRONTEND_DIR=$(pwd)
          else
            # Si on est déjà dans frontend ou il n'existe pas
            FRONTEND_DIR=$(pwd)
          fi
          
          # S'assurer que le répertoire assets/env existe
          mkdir -p "$FRONTEND_DIR/assets/env"
          
          # Créer directement le fichier dans assets/env/
          echo "Création du fichier d'environnement dans: $FRONTEND_DIR/assets/env"
          cat > "$FRONTEND_DIR/assets/env/production.env" << EOL
          # Configuration du serveur
          PORT=5000
          NODE_ENV=production
          
          # Configuration de MongoDB
          MONGO_URI=$CM_MONGO_URI
          
          # Configuration JWT
          JWT_SECRET=$CM_JWT_SECRET
          
          # Configuration des uploads
          UPLOAD_DIR=uploads
          MAX_FILE_SIZE=5242880
          
          # Configuration des APIs externes
          GOOGLE_MAPS_API_KEY=$CM_GOOGLE_MAPS_API_KEY
          STRIPE_SECRET_KEY=$CM_STRIPE_SECRET_KEY
          STRIPE_WEBHOOK_SECRET=$CM_STRIPE_WEBHOOK_SECRET
          OPENAI_API_KEY=$CM_OPENAI_API_KEY
          
          # Base URL pour l'API
          API_BASE_URL=https://api.choiceapp.fr
          WEBSOCKET_URL=wss://api.choiceapp.fr
          EOL
          
          # Copier aussi dans default.env pour être sûr
          cp "$FRONTEND_DIR/assets/env/production.env" "$FRONTEND_DIR/assets/env/default.env"
          
          echo "✅ Fichiers d'environnement créés avec succès pour l'environnement de build"
          
          # Retourner au répertoire du projet
          cd "$PROJECT_DIR"

      - name: Clean Flutter project
        script: |
          flutter clean
          flutter pub get
          flutter pub cache repair

      - name: Verify Custom Plugins
        script: |
          echo "🔍 Vérification des plugins personnalisés..."
          
          # Vérifier si les plugins personnalisés existent localement
          if [ -d "plugin" ]; then
            echo "✅ Dossier plugin trouvé"
            ls -la plugin/
          elif [ -d "../plugin" ]; then
            echo "✅ Dossier plugin trouvé au niveau parent"
            ls -la ../plugin/
          else
            echo "⚠️ Dossier de plugins personnalisés non trouvé" 
          fi
          
          # Ne pas tenter d'installer les plugins standards
          echo "⚙️ Utilisation des plugins personnalisés, pas d'installation depuis pub.dev"
          
          # Vérifier la présence des plugins dans pubspec.yaml
          echo "🔍 Vérification des références dans pubspec.yaml"
          grep -A 5 "firebase_core:" pubspec.yaml || echo "firebase_core non trouvé dans pubspec.yaml"
          grep -A 5 "cloud_firestore:" pubspec.yaml || echo "cloud_firestore non trouvé dans pubspec.yaml"
          grep -A 5 "firebase_messaging:" pubspec.yaml || echo "firebase_messaging non trouvé dans pubspec.yaml"
          grep -A 5 "firebase_analytics:" pubspec.yaml || echo "firebase_analytics non trouvé dans pubspec.yaml"
          grep -A 5 "firebase_auth:" pubspec.yaml || echo "firebase_auth non trouvé dans pubspec.yaml"

      - name: Ensure iOS deployment target is 14.0+ in Podfile
        script: |
          cd ios
          if [ -f "Podfile" ]; then
            # Ajouter explicitement la plateforme iOS 14.0
            sed -i '' 's/platform :ios, .*/platform :ios, "14.0"/' Podfile
            
            # Vérifier si la ligne platform existe, sinon l'ajouter
            if ! grep -q "platform :ios" Podfile; then
              # Ajouter au début du fichier
              sed -i '' '1i\
platform :ios, \'14.0\'' Podfile
            fi
            
            # Supprimer l'ancien hook pre_install s'il existe
            awk '/^# Hook pour résoudre/,/^end$/ {next} {print}' Podfile > Podfile.tmp && mv Podfile.tmp Podfile
            
            # Ajouter un hook pre_install pour gérer les podspecs manquants
            if ! grep -q "pre_install" Podfile; then
              cat >> Podfile << 'EOL'

# Hook pour résoudre les problèmes de podspecs manquants et garantir la compatibilité
pre_install do |installer|
  # S'assurer que la plateforme iOS est bien définie à 14.0 pour tous les pods
  installer.pods_project.targets.each do |target|
    target.build_configurations.each do |config|
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '14.0'
    end
  end
  
  # Gestion des podspecs manquants
  installer.pod_targets.each do |pod|
    # Vérifier si c'est un plugin Firebase ou autre connu pour poser problème
    if pod.name.start_with?('firebase_', 'cloud_firestore', 'add_2_calendar', 'webview_flutter')
      puts "Vérification du podspec pour: #{pod.name}"
      
      # Vérifier si le podspec est accessible
      unless File.exist?(File.join(pod.root, "#{pod.name}.podspec"))
        # Si le podspec n'existe pas, vérifier s'il existe dans ios/
        ios_podspec = File.join(pod.root, 'ios', "#{pod.name}.podspec")
        if !File.exist?(ios_podspec) && Dir.exist?(File.join(pod.root, 'ios'))
          # Créer un podspec générique si nécessaire
          File.open(ios_podspec, 'w') do |file|
            file.puts <<~PODSPEC
              Pod::Spec.new do |s|
                s.name             = '#{pod.name}'
                s.version          = '0.0.1'
                s.summary          = 'Flutter plugin for #{pod.name}'
                s.description      = 'Flutter plugin for #{pod.name}'
                s.homepage         = 'https://flutter.dev'
                s.license          = { :type => 'BSD', :file => '../LICENSE' }
                s.author           = { 'Flutter Team' => 'flutter-dev@googlegroups.com' }
                s.source           = { :path => '.' }
                s.source_files     = 'Classes/**/*'
                s.dependency 'Flutter'
                s.platform = :ios, '14.0'
                s.ios.deployment_target = '14.0'
                s.static_framework = true
                s.pod_target_xcconfig = { 'DEFINES_MODULE' => 'YES' }
              end
            PODSPEC
          end
          puts "✅ Podspec créé pour #{pod.name} dans #{ios_podspec}"
        end
      end
    end
  end
end

EOL
            fi

            # Ajouter aussi la définition explicite du projet Flutter
            if ! grep -q "flutter_root" Podfile; then
              cat >> Podfile << 'EOL'

# This is necessary to ensure proper integration with Flutter
flutter_root = File.dirname(File.realpath(`which flutter`.chomp))
flutter_ios_framework_path = File.join(flutter_root, 'bin', 'cache', 'artifacts', 'engine', 'ios')
flutter_framework_path = File.join(flutter_ios_framework_path, 'Flutter.xcframework')
if File.exist?(flutter_framework_path)
  pod 'Flutter', :path => flutter_framework_path
end

EOL
            fi

            echo "✅ Podfile configuré avec iOS 14.0 et hooks de compatibilité"
            cat Podfile
          else
            echo "🚨 Podfile non trouvé ! Assure-toi que ton projet Flutter iOS est bien configuré."
            exit 1
          fi

      - name: Update CocoaPods
        script: |
          sudo gem install cocoapods
          pod repo update

      - name: Manual Podspec Creation for Custom Firebase Plugins
        script: |
          echo "🔧 Création de podspecs manuels pour les plugins Firebase personnalisés..."
          cd ios
          
          # Base directory for plugin symlinks
          SYMLINK_DIR=".symlinks/plugins"
          
          # Fonction pour créer un podspec
          create_podspec() {
            PLUGIN_NAME=$1
            PLUGIN_PATH="$SYMLINK_DIR/$PLUGIN_NAME/ios"
            PODSPEC_FILE="$PLUGIN_PATH/$PLUGIN_NAME.podspec"
            FIREBASE_DEPENDENCY=$2
            
            mkdir -p "$PLUGIN_PATH"
            
            if [ ! -f "$PODSPEC_FILE" ]; then
              echo "📝 Création du podspec pour $PLUGIN_NAME à $PODSPEC_FILE"
              cat > "$PODSPEC_FILE" <<- EOL
Pod::Spec.new do |s|
  s.name             = '$PLUGIN_NAME'
  s.version          = '0.0.1' # Version factice
  s.summary          = 'Custom $PLUGIN_NAME plugin for Flutter.'
  s.description      = 'Custom $PLUGIN_NAME plugin for Flutter, using local source.'
  s.homepage         = 'https://github.com/RemiBp/flutter_app_badger_fork'
  s.license          = { :type => 'BSD', :file => '../../../LICENSE' } # Ajuster le chemin si nécessaire
  s.author           = { 'Choice App' => 'remi.barbier@hec.edu' }
  s.source           = { :path => '.' } # Le source est le répertoire ios du plugin
  s.source_files     = 'Classes/**/*'
  s.public_header_files = 'Classes/**/*.h'
  s.ios.deployment_target = '14.0'
  s.static_framework = true
  s.dependency 'Flutter'
  s.dependency 'firebase_core' # Dépendance de base
  s.dependency '$FIREBASE_DEPENDENCY', '~> 10.9.0' # Dépendance Firebase spécifique
end
EOL
            else
                echo "✅ Podspec pour $PLUGIN_NAME existe déjà."
            fi
          }

          # Fonction pour créer un lien symbolique entre le nom standard et le nom personnalisé
          create_symlink() {
            STANDARD_NAME=$1
            CUSTOM_NAME=$2
            
            STANDARD_PATH="$SYMLINK_DIR/$STANDARD_NAME"
            CUSTOM_PATH="$SYMLINK_DIR/$CUSTOM_NAME"
            
            mkdir -p "$STANDARD_PATH/ios"
            mkdir -p "$CUSTOM_PATH/ios"
            
            # Créer le podspec pour le nom standard
            create_podspec "$STANDARD_NAME" "$3"
            
            # Créer le podspec pour le nom personnalisé
            create_podspec "$CUSTOM_NAME" "$3"
            
            echo "🔄 Liaison entre $STANDARD_NAME et $CUSTOM_NAME créée"
          }
          
          # Créer les symlinks et podspecs pour les paires standard/personnalisé
          create_symlink "firebase_core" "firebase_core_custom" "Firebase/CoreOnly"
          create_symlink "cloud_firestore" "cloud_firestore_custom" "Firebase/Firestore"
          create_symlink "firebase_messaging" "firebase_messaging_custom" "Firebase/Messaging"
          create_symlink "firebase_analytics" "firebase_analytics_custom" "Firebase/Analytics"
          create_symlink "firebase_auth" "firebase_auth_custom" "Firebase/Auth"
          
          # Créer aussi des podspecs pour d'autres plugins qui peuvent poser problème
          echo "🔧 Création de podspecs additionnels pour les plugins problématiques..."
          for PLUGIN in "add_2_calendar" "webview_flutter" "emoji_picker_flutter"; do
            PLUGIN_PATH="$SYMLINK_DIR/$PLUGIN/ios"
            PODSPEC_FILE="$PLUGIN_PATH/$PLUGIN.podspec"
            
            mkdir -p "$PLUGIN_PATH"
            
            if [ ! -f "$PODSPEC_FILE" ]; then
              echo "📝 Création du podspec générique pour $PLUGIN à $PODSPEC_FILE"
              cat > "$PODSPEC_FILE" <<- EOL
Pod::Spec.new do |s|
  s.name             = '$PLUGIN'
  s.version          = '0.0.1'
  s.summary          = 'Flutter plugin for $PLUGIN'
  s.description      = 'Flutter plugin for $PLUGIN'
  s.homepage         = 'https://flutter.dev'
  s.license          = { :type => 'BSD', :file => '../../../LICENSE' }
  s.author           = { 'Flutter Team' => 'flutter-dev@googlegroups.com' }
  s.source           = { :path => '.' }
  s.source_files     = 'Classes/**/*'
  s.public_header_files = 'Classes/**/*.h'
  s.dependency 'Flutter'
  s.platform = :ios, '14.0'
  s.pod_target_xcconfig = { 'DEFINES_MODULE' => 'YES', 'EXCLUDED_ARCHS[sdk=iphonesimulator*]' => 'i386' }
end
EOL
            else
              echo "✅ Podspec pour $PLUGIN existe déjà."
            fi
          done
          
          echo "✅ Vérification/Création des podspecs manuels terminée"
          ls -la $SYMLINK_DIR/*/ios/*.podspec || echo "Aucun podspec personnalisé trouvé/créé"

      - name: Remove and reinstall CocoaPods dependencies
        script: |
          cd ios
          rm -rf Pods Podfile.lock
          pod deintegrate || echo "Pod deintegrate skipped or failed, continuing..."
          
          # Vérifier les chemins des plugins
          echo "🔍 Vérification des dossiers de plugins importants..."
          SYMLINK_DIR=".symlinks/plugins"
          for PLUGIN in "firebase_core" "cloud_firestore" "firebase_core_custom" "cloud_firestore_custom" "add_2_calendar"; do
            echo "  - Checking $PLUGIN..."
            if [ -d "$SYMLINK_DIR/$PLUGIN" ]; then
              echo "    ✅ $PLUGIN directory exists"
              find "$SYMLINK_DIR/$PLUGIN" -name "*.podspec" | xargs ls -la || echo "    ⚠️ No podspec files found"
            else
              echo "    ❌ $PLUGIN directory missing"
            fi
          done
          
          # Forcer l'installation des pods
          echo "🚀 Tentative d'installation des pods..."
          COCOAPODS_DISABLE_STATS=true pod install --repo-update --verbose
          
          # Si l'installation échoue, essayer avec --allow-root et autres options
          if [ $? -ne 0 ]; then
            echo "⚠️ Première tentative d'installation des pods échouée, essai avec options alternatives..."
            COCOAPODS_DISABLE_STATS=true pod install --repo-update --verbose --allow-root
            
            # Si ça échoue encore, essayer avec --no-clean
            if [ $? -ne 0 ]; then
              echo "⚠️ Seconde tentative échouée, essai avec --no-clean..."
              COCOAPODS_DISABLE_STATS=true pod install --repo-update --verbose --no-clean
            fi
          fi
          
          # Vérifier si l'installation a réussi
          if [ -d "Pods" ] && [ -f "Podfile.lock" ]; then
             echo "✅ Installation des pods réussie."
          else
             echo "❌ Échec de l'installation des pods après plusieurs tentatives."
             # Afficher le contenu du répertoire pour le débogage
             ls -la
             
             # Tenter une approche alternative avec bundle
             echo "🔄 Tentative avec bundler..."
             sudo gem install bundler
             bundle init
             echo "gem 'cocoapods', '1.12.1'" >> Gemfile
             bundle install
             bundle exec pod install --repo-update
             
             # Vérifier à nouveau
             if [ -d "Pods" ] && [ -f "Podfile.lock" ]; then
                echo "✅ Installation des pods réussie avec bundler."
             else
                exit 1
             fi
          fi
          
          # Afficher le Pods/Manifest.lock pour déboguer
          if [ -f "Pods/Manifest.lock" ]; then
            echo "📋 Contenu de Pods/Manifest.lock:"
            head -n 50 Pods/Manifest.lock # Afficher les 50 premières lignes
          else
            echo "⚠️ Pods/Manifest.lock non trouvé"
          fi

      - name: Verify Apple Pay Merchant ID in Entitlements
        script: |
          cd ios
          if [ ! -f "Runner/Runner.entitlements" ]; then
            cat > Runner/Runner.entitlements << EOL
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.developer.in-app-payments</key>
    <array>
        <string>merchant.fr.choiceapp.app</string>
    </array>
</dict>
</plist>
EOL
            echo "✅ Fichier d'entitlements Apple Pay créé"
          else
            echo "✅ Fichier d'entitlements existant"
          fi
          if ! grep -q "merchant.fr.choiceapp.app" Runner/Info.plist; then
            plutil -insert 'com.apple.developer.in-app-payments' -xml '<array><string>merchant.fr.choiceapp.app</string></array>' Runner/Info.plist || true
            echo "✅ Merchant ID ajouté à Info.plist"
          fi

      - name: Apply iOS 14.0 Deployment Fix in Xcode Info.plist
        script: |
          cd ios
          plutil -replace MinimumOSVersion -string "14.0" Runner/Info.plist
          plutil -replace LSRequiresIPhoneOS -bool YES Runner/Info.plist

      - name: Install certificate in keychain
        script: |
          echo $CM_CERTIFICATE | base64 --decode > ios_distribution.p12
          security create-keychain -p "" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "" build.keychain
          security import ios_distribution.p12 -k build.keychain -P "$CM_CERTIFICATE_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple: -s -k "" build.keychain
          echo "✅ Certificat installé dans le keychain"

      - name: Debug keychain and provisioning
        script: |
          echo "== Profils de provisionnement =="
          ls -l ~/Library/MobileDevice/Provisioning\ Profiles/
          echo "== Certificats dans le keychain =="
          security find-identity -v -p codesigning
          echo "== Team ID utilisé =="
          echo $CM_APP_STORE_CONNECT_TEAM_ID

      - name: Build iOS IPA with Proper Signing
        script: |
          flutter build ipa --release
          echo "✅ Build iOS avec signature terminé"
          if [ -f "build/ios/ipa/app-release.ipa" ]; then
            echo "🎉 IPA signé généré avec succès"
          else
            echo "🔍 Recherche d'autres IPAs générés..."
            find build -name "*.ipa"
          fi

    artifacts:
      - build/ios/ipa/*.ipa

    publishing:
      email:
        recipients:
          - "remi.barbier@hec.edu"

  ios-workflow:
    name: iOS Workflow
    instance_type: mac_mini_m1
    max_build_duration: 180
    environment:
      flutter: stable
      xcode: latest
      cocoapods: latest
      ios_signing:
        distribution_type: ad-hoc
        bundle_identifier: fr.choiceapp.app
    scripts:
      - name: Clone des plugins personnalisés
        script: |
          echo "Clonage des plugins personnalisés..."
          if [ ! -d "plugin" ]; then
            mkdir -p plugin
            git clone --depth 1 --branch main https://github.com/RemiBp/flutter_app_badger_fork.git plugin
            echo "✅ Plugins personnalisés clonés avec succès"
          else
            echo "✅ Répertoire plugin existe déjà"
          fi

      - name: Setup iOS Plugin Dependencies
        script: |
          echo "Setting up iOS plugin dependencies..."
          cd plugin
          
          # Vérifier l'existence des fichiers .podspec
          if [ ! -f "cloud_firestore_custom/cloud_firestore.podspec" ]; then
            echo "Creating firebase podspecs..."
            
            # cloud_firestore
            cat > cloud_firestore_custom/cloud_firestore.podspec << EOL
require 'yaml'
podspec_path = File.join(__dir__, 'pubspec.yaml')
if File.exist?(podspec_path)
  pubspec = YAML.load_file(podspec_path)
else
  pubspec = { 'version' => '0.0.1' }
end

Pod::Spec.new do |s|
  s.name             = 'cloud_firestore'
  s.version          = '0.0.1'
  s.summary          = 'Flutter Cloud Firestore'
  s.description      = 'Flutter plugin for Cloud Firestore, a cloud-hosted, noSQL database with live synchronization and offline support on Android and iOS.'
  s.homepage         = 'https://github.com/firebase/flutterfire/tree/master/packages/cloud_firestore'
  s.license          = { :type => 'BSD', :file => '../LICENSE' }
  s.author           = { 'Flutter Team' => 'flutter-dev@googlegroups.com' }
  s.source           = { :path => '.' }
  s.source_files = 'ios/**/*.{h,m}'
  s.public_header_files = 'ios/**/*.h'
  s.ios.deployment_target = '13.0'
  s.dependency 'Flutter'
  s.dependency 'firebase_core'
  s.dependency 'Firebase/Firestore', '~> 10.12.0'
  s.static_framework = true
  s.pod_target_xcconfig = { 'GCC_PREPROCESSOR_DEFINITIONS' => "LIBRARY_VERSION=\\@\\\"#{pubspec['version']}\\\" LIBRARY_NAME=\\@\\\"flutter-fire-fst\\\"" }
end
EOL

            # firebase_core
            cat > firebase_core_custom/firebase_core.podspec << EOL
require 'yaml'
podspec_path = File.join(__dir__, 'pubspec.yaml')
if File.exist?(podspec_path)
  pubspec = YAML.load_file(podspec_path)
else
  pubspec = { 'version' => '0.0.1' }
end

Pod::Spec.new do |s|
  s.name             = 'firebase_core'
  s.version          = '0.0.1'
  s.summary          = 'Flutter Firebase Core'
  s.description      = 'Flutter plugin for Firebase Core, enabling connecting to multiple Firebase apps.'
  s.homepage         = 'https://github.com/firebase/flutterfire/tree/master/packages/firebase_core'
  s.license          = { :type => 'BSD', :file => '../LICENSE' }
  s.author           = { 'Flutter Team' => 'flutter-dev@googlegroups.com' }
  s.source           = { :path => '.' }
  s.source_files = 'ios/**/*.{h,m}'
  s.public_header_files = 'ios/**/*.h'
  s.ios.deployment_target = '13.0'
  s.dependency 'Flutter'
  s.dependency 'Firebase/CoreOnly', '~> 10.12.0'
  s.static_framework = true
  s.pod_target_xcconfig = { 'GCC_PREPROCESSOR_DEFINITIONS' => "LIBRARY_VERSION=\\@\\\"#{pubspec['version']}\\\" LIBRARY_NAME=\\@\\\"flutter-fire-core\\\"" }
end
EOL

            # firebase_messaging
            cat > firebase_messaging_custom/firebase_messaging.podspec << EOL
require 'yaml'
podspec_path = File.join(__dir__, 'pubspec.yaml')
if File.exist?(podspec_path)
  pubspec = YAML.load_file(podspec_path)
else
  pubspec = { 'version' => '0.0.1' }
end

Pod::Spec.new do |s|
  s.name             = 'firebase_messaging'
  s.version          = '0.0.1'
  s.summary          = 'Flutter Firebase Cloud Messaging'
  s.description      = 'Flutter plugin for Firebase Cloud Messaging, a cross-platform messaging solution that lets you reliably deliver messages on Android and iOS.'
  s.homepage         = 'https://github.com/firebase/flutterfire/tree/master/packages/firebase_messaging'
  s.license          = { :type => 'BSD', :file => '../LICENSE' }
  s.author           = { 'Flutter Team' => 'flutter-dev@googlegroups.com' }
  s.source           = { :path => '.' }
  s.source_files = 'ios/**/*.{h,m}'
  s.public_header_files = 'ios/**/*.h'
  s.ios.deployment_target = '13.0'
  s.dependency 'Flutter'
  s.dependency 'firebase_core'
  s.dependency 'Firebase/Messaging', '~> 10.12.0'
  s.static_framework = true
  s.pod_target_xcconfig = { 'GCC_PREPROCESSOR_DEFINITIONS' => "LIBRARY_VERSION=\\@\\\"#{pubspec['version']}\\\" LIBRARY_NAME=\\@\\\"flutter-fire-fcm\\\"" }
end
EOL

            # firebase_analytics
            cat > firebase_analytics_custom/firebase_analytics.podspec << EOL
require 'yaml'
podspec_path = File.join(__dir__, 'pubspec.yaml')
if File.exist?(podspec_path)
  pubspec = YAML.load_file(podspec_path)
else
  pubspec = { 'version' => '0.0.1' }
end

Pod::Spec.new do |s|
  s.name             = 'firebase_analytics'
  s.version          = '0.0.1'
  s.summary          = 'Flutter Firebase Analytics'
  s.description      = 'Flutter plugin for Google Analytics for Firebase, an app measurement solution that provides insight on app usage and user engagement on Android and iOS.'
  s.homepage         = 'https://github.com/firebase/flutterfire/tree/master/packages/firebase_analytics'
  s.license          = { :type => 'BSD', :file => '../LICENSE' }
  s.author           = { 'Flutter Team' => 'flutter-dev@googlegroups.com' }
  s.source           = { :path => '.' }
  s.source_files = 'ios/**/*.{h,m}'
  s.public_header_files = 'ios/**/*.h'
  s.ios.deployment_target = '13.0'
  s.dependency 'Flutter'
  s.dependency 'firebase_core'
  s.dependency 'Firebase/Analytics', '~> 10.12.0'
  s.static_framework = true
  s.pod_target_xcconfig = { 'GCC_PREPROCESSOR_DEFINITIONS' => "LIBRARY_VERSION=\\@\\\"#{pubspec['version']}\\\" LIBRARY_NAME=\\@\\\"flutter-fire-analytics\\\"" }
end
EOL

            # firebase_auth
            cat > firebase_auth_custom/firebase_auth.podspec << EOL
require 'yaml'
podspec_path = File.join(__dir__, 'pubspec.yaml')
if File.exist?(podspec_path)
  pubspec = YAML.load_file(podspec_path)
else
  pubspec = { 'version' => '0.0.1' }
end

Pod::Spec.new do |s|
  s.name             = 'firebase_auth'
  s.version          = '0.0.1'
  s.summary          = 'Flutter Firebase Auth'
  s.description      = 'Flutter plugin for Firebase Auth, enabling authentication using passwords, phone numbers and identity providers like Google, Facebook and Twitter.'
  s.homepage         = 'https://github.com/firebase/flutterfire/tree/master/packages/firebase_auth'
  s.license          = { :type => 'BSD', :file => '../LICENSE' }
  s.author           = { 'Flutter Team' => 'flutter-dev@googlegroups.com' }
  s.source           = { :path => '.' }
  s.source_files = 'ios/**/*.{h,m}'
  s.public_header_files = 'ios/**/*.h'
  s.ios.deployment_target = '13.0'
  s.dependency 'Flutter'
  s.dependency 'firebase_core'
  s.dependency 'Firebase/Auth', '~> 10.12.0'
  s.static_framework = true
  s.pod_target_xcconfig = { 'GCC_PREPROCESSOR_DEFINITIONS' => "LIBRARY_VERSION=\\@\\\"#{pubspec['version']}\\\" LIBRARY_NAME=\\@\\\"flutter-fire-auth\\\"" }
end
EOL
          fi
          
          cd ..
          
      - name: Setup iOS Podfile
        script: |
          echo "Setting up iOS Podfile..."
          cd ios
          if [ -f "Podfile" ]; then
            rm Podfile
          fi
          
          cat > Podfile << EOL
# Uncomment this line to define a global platform for your project
platform :ios, '13.0'

# CocoaPods analytics sends network stats synchronously affecting flutter build latency.
ENV['COCOAPODS_DISABLE_STATS'] = 'true'

project 'Runner', {
  'Debug' => :debug,
  'Profile' => :release,
  'Release' => :release,
}

def flutter_root
  generated_xcode_build_settings_path = File.expand_path(File.join('..', 'Flutter', 'Generated.xcconfig'), __FILE__)
  unless File.exist?(generated_xcode_build_settings_path)
    raise "#{generated_xcode_build_settings_path} must exist. If you're running pod install manually, make sure flutter pub get is executed first"
  end

  File.foreach(generated_xcode_build_settings_path) do |line|
    matches = line.match(/FLUTTER_ROOT\=(.*)/)
    return matches[1].strip if matches
  end
  raise "FLUTTER_ROOT not found in #{generated_xcode_build_settings_path}. Try deleting Generated.xcconfig, then run flutter pub get"
end

require File.expand_path(File.join('packages', 'flutter_tools', 'bin', 'podhelper'), flutter_root)

flutter_ios_podfile_setup

# Add custom plugin paths - these should match where your plugin source code is located
plugin_pods_path = '../../plugin'

target 'Runner' do
  use_frameworks!
  use_modular_headers!

  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))

  # Link to local Firebase if needed
  pod 'Firebase/CoreOnly', '~> 10.12.0'
  pod 'Firebase/Firestore', '~> 10.12.0'
  pod 'Firebase/Messaging', '~> 10.12.0'
  pod 'Firebase/Analytics', '~> 10.12.0'
  pod 'Firebase/Auth', '~> 10.12.0'
end

post_install do |installer|
  installer.pods_project.targets.each do |target|
    flutter_additional_ios_build_settings(target)
    
    # Workaround for iOS build issues
    target.build_configurations.each do |config|
      # Set minimum iOS version correctly
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '13.0'
      
      # Other build settings that may help with compatibility
      config.build_settings['ENABLE_BITCODE'] = 'NO'
      
      # Exclude arm64 architecture for simulator builds to fix M1 Mac issues
      config.build_settings['EXCLUDED_ARCHS[sdk=iphonesimulator*]'] = 'arm64'
    end
  end
end
EOL
          cd ..
          
      - name: Mettre à jour le pubspec.yaml pour utiliser les commits a69a3dd
        script: |
          echo "Mise à jour du pubspec.yaml pour utiliser les commits a69a3dd..."
          # Supprimer toutes les références à 7cf150a et les remplacer par a69a3dd
          sed -i '' 's/ref: "7cf150a"/ref: "a69a3dd"/g' pubspec.yaml
          sed -i '' 's/ref: "72ddd21"/ref: "a69a3dd"/g' pubspec.yaml
          sed -i '' 's/ref: "e3450f7"/ref: "a69a3dd"/g' pubspec.yaml
          echo "✅ pubspec.yaml mis à jour"
          
      - name: Clean and Install Pods
        script: |
          # Nettoyage Flutter
          flutter clean
          flutter pub get
          
          # Installation des pods
          cd ios
          rm -rf Pods
          rm -f Podfile.lock
          pod install --verbose
          cd ..
          
      - name: Vérifier les structures de pods
        script: |
          echo "Vérification des structures de pods..."
          cd ios
          if [ -d "Pods" ]; then
            echo "✅ Répertoire Pods existe"
            ls -la Pods/
            
            # Vérifier les pods Firebase
            echo "🔍 Vérification des pods Firebase..."
            if [ -d "Pods/Firebase" ]; then
              echo "✅ Firebase trouvé"
              ls -la Pods/Firebase/
            else
              echo "❌ Firebase non trouvé"
            fi
            
            # Vérifier cloud_firestore
            echo "🔍 Vérification de cloud_firestore..."
            if [ -d "Pods/cloud_firestore" ]; then
              echo "✅ cloud_firestore trouvé"
              ls -la Pods/cloud_firestore/
            else
              echo "❌ cloud_firestore non trouvé"
            fi
          else
            echo "❌ Répertoire Pods non trouvé"
          fi
          cd ..

      - name: Build iOS IPA
        script: |
          # Build iOS
          flutter build ios --release --no-codesign
          
          # Pour le débogage, vérifier si les frameworks Firebase sont inclus
          echo "Vérification des frameworks Firebase dans le build..."
          find build/ios -name "*Firebase*" | sort

      - name: Create IPA File
        script: |
          # Créer IPA pour les tests
          cd build/ios/iphoneos
          mkdir -p Payload
          cp -r Runner.app Payload/
          zip -r app.ipa Payload
          echo "✅ IPA créé"
          ls -la app.ipa

    artifacts:
      - build/ios/iphoneos/app.ipa
      - ios/Pods/Manifest.lock
      - ios/Podfile
      - ios/Podfile.lock
      - flutter_drive.log
      
    publishing:
      email:
        recipients:
          - "remi.barbier@hec.edu"
